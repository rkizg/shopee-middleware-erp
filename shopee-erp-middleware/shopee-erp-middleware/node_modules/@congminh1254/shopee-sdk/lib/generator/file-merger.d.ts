import { EndpointSpec } from "./types.js";
import { AstBuilder } from "./ast-builder.js";
export declare class FileMerger {
    private astBuilder;
    private printer;
    constructor(astBuilder: AstBuilder);
    mergeSchemaFile(filePath: string, specs: EndpointSpec[]): void;
    mergeManagerFile(filePath: string, specs: EndpointSpec[], moduleName: string): void;
}
