export function toPascalCase(str) {
    if (str.toLowerCase() === "livestream")
        return "LiveStream";
    const normalized = str.normalize("NFD").replace(/[\u0300-\u036f]/g, "");
    return normalized
        .replace(/[^a-zA-Z0-9_\s-]/g, "")
        .split(/[\s_-]+/)
        .map((word) => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase())
        .join("");
}
export function toCamelCase(str) {
    if (str.toLowerCase() === "livestream")
        return "livestream";
    const pascal = toPascalCase(str);
    return pascal.charAt(0).toLowerCase() + pascal.slice(1);
}
export function getManagerClassName(moduleName) {
    if (moduleName.toLowerCase() === "livestream")
        return "LiveStreamManager";
    return `${toPascalCase(moduleName)}Manager`;
}
export function getSdkPropertyName(moduleName) {
    if (moduleName.toLowerCase() === "livestream")
        return "livestream";
    return toCamelCase(moduleName);
}
//# sourceMappingURL=types.js.map